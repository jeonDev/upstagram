import {Card} from "react-bootstrap";

const SearchInfoList = (props) => {
    const {searchInfoList} = props;

    return (
        <Card>
            <div>

            </div>
        </Card>
    )
}

export default SearchInfoList;